---

- name: Install Nginx
  apt:
    name: nginx
    state: present

- name: Copy Reverse Proxy Configuration
  template:
    src: default.conf.j2
    dest: /etc/nginx/sites-available/default

  notify:
    - Restart Nginx

- name: Enable Nginx
  service:
    name: nginx
    state: started
    enabled: yes